PROGRAM PRE;

FUNCTION GETB(X: BOOLEAN): BOOLEAN;
BEGIN
    GETB := X
END;

FUNCTION GETI(X: INTEGER): INTEGER;
BEGIN
    GETI := X
END;

VAR X, Y, Z;
BEGIN
    X := GETI(1);
    IF GETB(TRUE) THEN Y := X + 4 ELSE Y := 0;
    Z := X + 4;
    WRITELN(Z);
    WRITELN(Y)
END.
